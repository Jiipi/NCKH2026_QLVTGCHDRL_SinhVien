/**
 * Activities UI Layer - Barrel Export
 * Combines shared components and pages
 */

// Shared Components (dùng chung cho tất cả roles)
export * from './shared';

// Pages (shared pages: detail, manage, class-activities)
export * from './pages';
