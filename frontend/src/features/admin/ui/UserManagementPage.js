/**
 * Admin User Management Page (FSD Structure)
 * 
 * Temporarily importing from legacy page to ensure no breaking changes
 * TODO: Fully migrate to FSD structure with separate components
 */

// Migrated: use FSD AdminUsersPage directly (legacy bridge removed)
import AdminUsersPage from './AdminUsersPage';
export default AdminUsersPage;
