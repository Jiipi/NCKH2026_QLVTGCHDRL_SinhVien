╔════════════════════════════════════════════════════════════════╗
║         PRODUCTION DEPLOYMENT - QUICK START                    ║
║         Domain: hoatdongrenluyen.io.vn                        ║
║         Platform: AWS EC2 Amazon Linux 2                      ║
║         User: ec2-user | Path: /home/ec2-user/app            ║
╚════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│ 1. SSH VÀO EC2                                               │
└──────────────────────────────────────────────────────────────┘

ssh -i hoatdongrenluyen-key.pem ec2-user@[EC2_PUBLIC_IP]

┌──────────────────────────────────────────────────────────────┐
│ 2. CHẠY SETUP SCRIPT (1 LẦN DUY NHẤT)                        │
└──────────────────────────────────────────────────────────────┘

curl -o setup.sh https://raw.githubusercontent.com/Jiipi/QL_DH_RenLuyen/main/SETUP_EC2.sh
bash setup.sh

→ Copy SSH key hiển thị
→ Add vào GitHub: Settings → SSH and GPG keys → New SSH key

┌──────────────────────────────────────────────────────────────┐
│ 3. ĐỔI PASSWORD TRONG .ENV                                   │
└──────────────────────────────────────────────────────────────┘

nano /home/ec2-user/app/.env

Đổi 3 dòng:
  DB_PASSWORD=...
  DATABASE_URL=postgresql://admin:NEW_PASSWORD@db:5432/...
  JWT_SECRET=...

Generate password: openssl rand -base64 32

Save: Ctrl+O → Enter → Ctrl+X

┌──────────────────────────────────────────────────────────────┐
│ 4. CẤU HÌNH DNS                                              │
└──────────────────────────────────────────────────────────────┘

Vào nhà cung cấp domain, thêm 2 A records:

┌────────┬──────┬────────────────────┐
│ Type   │ Name │ Value              │
├────────┼──────┼────────────────────┤
│ A      │ @    │ [EC2_PUBLIC_IP]    │
│ A      │ www  │ [EC2_PUBLIC_IP]    │
└────────┴──────┴────────────────────┘

Test: nslookup hoatdongrenluyen.io.vn

┌──────────────────────────────────────────────────────────────┐
│ 5. DEPLOY 4 CONTAINERS                                       │
└──────────────────────────────────────────────────────────────┘

cd /home/ec2-user/app
docker compose -f docker-compose.production.yml up -d

Đợi 2-3 phút...

Check containers:
docker compose -f docker-compose.production.yml ps

Phải có 5 containers Up:
  ✓ hoatdongrenluyen_db
  ✓ hoatdongrenluyen_backend
  ✓ hoatdongrenluyen_frontend
  ✓ hoatdongrenluyen_nginx
  ✓ hoatdongrenluyen_certbot

┌──────────────────────────────────────────────────────────────┐
│ 6. SETUP SSL CERTIFICATE                                     │
└──────────────────────────────────────────────────────────────┘

sudo certbot --nginx -d hoatdongrenluyen.io.vn -d www.hoatdongrenluyen.io.vn

Nhập:
  → Email address
  → Y (agree terms)
  → Y (redirect HTTP to HTTPS)

┌──────────────────────────────────────────────────────────────┐
│ 7. TEST                                                       │
└──────────────────────────────────────────────────────────────┘

Browser: https://hoatdongrenluyen.io.vn

✓ Website load
✓ Icon khóa xanh (SSL)
✓ Login works

╔════════════════════════════════════════════════════════════════╗
║                     ✅ HOÀN THÀNH!                            ║
╚════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│ CI/CD AUTO-DEPLOY (OPTIONAL)                                │
└──────────────────────────────────────────────────────────────┘

GitHub → Settings → Secrets → Actions

Add 2 secrets:
  1. EC2_HOST = [EC2_PUBLIC_IP]
  2. EC2_SSH_KEY = [Nội dung file .pem]

→ Mỗi lần push code → Tự động deploy!

┌──────────────────────────────────────────────────────────────┐
│ LỆNH THƯỜNG DÙNG                                             │
└──────────────────────────────────────────────────────────────┘

# Xem containers
docker compose -f docker-compose.production.yml ps

# Xem logs realtime
docker compose -f docker-compose.production.yml logs -f

# Restart service
docker compose -f docker-compose.production.yml restart backend

# Stop tất cả
docker compose -f docker-compose.production.yml down

# Update code (hoặc đợi GitHub Actions)
cd /home/ec2-user/app
git pull
docker compose -f docker-compose.production.yml up -d --build

# Backup database
docker compose -f docker-compose.production.yml exec db \
  pg_dump -U admin Web_QuanLyDiemRenLuyen > backup.sql

┌──────────────────────────────────────────────────────────────┐
│ CẤU TRÚC THƯ MỤC                                            │
└──────────────────────────────────────────────────────────────┘

/home/ec2-user/app/
├── .env                              ← Biến môi trường
├── docker-compose.production.yml     ← 4 containers
├── SETUP_EC2.sh                      ← Script setup
├── backend/
│   ├── Dockerfile.production         ← Build backend
│   ├── src/                          ← Source code
│   ├── prisma/                       ← Database schema
│   ├── logs/                         ← Application logs
│   └── uploads/                      ← User uploads
├── frontend/
│   ├── Dockerfile.production         ← Build React
│   └── src/                          ← React code
├── nginx/
│   ├── nginx-production.conf         ← Reverse proxy
│   └── logs/                         ← Access logs
└── backups/                          ← Database backups

┌──────────────────────────────────────────────────────────────┐
│ 4 CONTAINERS ARCHITECTURE                                    │
└──────────────────────────────────────────────────────────────┘

Internet
   ↓
┌──────────────────────────────────────┐
│ NGINX (Port 80, 443)                │  ← Reverse Proxy + SSL
│ hoatdongrenluyen_nginx               │
└─────────────┬────────────────────────┘
              │
         ┌────┴────┐
         ↓         ↓
    ┌─────────┐  ┌──────────────┐
    │ FRONTEND│  │ BACKEND      │
    │ Port 80 │  │ Port 3001    │
    └─────────┘  └──────┬───────┘
                        │
                        ↓
                 ┌──────────────┐
                 │ DATABASE     │
                 │ PostgreSQL   │
                 │ Port 5432    │
                 └──────────────┘

┌──────────────────────────────────────────────────────────────┐
│ TROUBLESHOOT                                                 │
└──────────────────────────────────────────────────────────────┘

Website không load:
  → docker compose -f docker-compose.production.yml logs nginx
  → sudo ufw status
  → nslookup hoatdongrenluyen.io.vn

Database lỗi:
  → docker compose -f docker-compose.production.yml logs db
  → docker compose -f docker-compose.production.yml exec db psql -U admin

Rebuild tất cả:
  → docker compose -f docker-compose.production.yml down
  → docker compose -f docker-compose.production.yml up -d --build

╔════════════════════════════════════════════════════════════════╗
║  📚 ĐỌC THÊM:                                                ║
║  → DEPLOY.md       (Chi tiết từng bước)                      ║
║  → CHECKLIST.md    (Checklist đầy đủ)                        ║
║  → CICD.md         (Setup GitHub Actions)                    ║
╚════════════════════════════════════════════════════════════════╝

Total time: ~30 phút
Domain: https://hoatdongrenluyen.io.vn
User: ec2-user
Path: /home/ec2-user/app
